import 'package:flutter/material.dart';

class Cores {
  static Color? corfundo = Colors.grey[200];
  static Color corprincipalescura = Color.fromRGBO(42, 108, 158, 1);
  static Color corprincipal = Colors.teal;
  static Color corlink = Color.fromRGBO(41, 135, 63, 1);
  static Color corevento1 = Color.fromRGBO(0, 200, 157, 1);
  static Color corevento2 = Color.fromRGBO(229, 137, 66, 1);
  static Color? corquadrorecado = Colors.green[900];
  static Color? cormensagem = Colors.teal[800];
  static Color cortextorecado = Colors.white;
  static Color corbilhetes = Color.fromRGBO(209, 0, 116, 1.0);
  static Color corenquete = Color.fromRGBO(255, 145, 51, 1.0).withOpacity(0.6);
  static Color corcuidado = Colors.green.withOpacity(0.2);
  static Color corpretatitulo = Colors.black;
}